# Operations

מסמך קצר להפעלה שוטפת של מקור בדיקה.

## לפני פרסום עמוד חדש

לפני תחילת העבודה להריץ `npm run check:content`. לאחר הוספת העמוד להריץ שוב כדי לוודא התאמה בין העמוד, תמונת השיתוף, האשכול וה-sitemap.

1. לוודא שהעמוד עונה על טענה אחת ברורה.
2. לוודא שיש תשובה קצרה בתחילת העמוד.
3. לוודא שיש הפרדה בין עובדה, פרשנות, אפשרות ומסקנה.
4. לוודא שכל מקור פתוח בקישור ישיר.
5. לוודא שאין שימוש בתשובת AI כמקור.
6. לוודא שיש תאריך עדכון.
7. לוודא שיש תגובה קצרה להעתקה.
8. להוסיף רשומה ל-`lib/claims-db.ts`.
9. להוסיף `tags` לרשומת ה-claim.
10. לוודא שיש `opengraph-image.tsx` לעמוד.
11. אם העמוד צריך להופיע כבדיקה לדוגמה בהום, לשנות את `homeFeaturedClaimSlug`.

## אחרי פרסום עמוד חדש

1. לבדוק שהעמוד מחזיר 200.
2. לבדוק שאין noindex.
3. לבדוק canonical.
4. לוודא שהעמוד מופיע ב-`/sitemap.xml`.
5. לוודא שהעמוד מופיע ב-`/llms.txt`, כולל תגיות.
6. לוודא שהעמוד מופיע ב-`/topics#all-checks`.
7. לבדוק שהוא מופיע בחיפוש החי ב-`/topics#find-check`.
8. לבדוק שהתגיות מוצגות בכרטיסים ולא שוברות מובייל.
9. לבדוק שה-OG עובד ושעברית לא נשברת בתמונה.
10. להגיש את ה-URL ל-IndexNow.
11. לבקש בדיקת URL ב-Bing Webmaster Tools.
12. לבקש בדיקת URL ב-Google Search Console רק כשהכלי זמין.
13. לא לשלוח בקשות חוזרות בלופים. להמתין לפחות 24 שעות.

## מודל שימוש לציבור

חשוב שהאתר לא ישדר שהוא צ׳אט או כלי שמחזיר בדיקה אוטומטית לכל פרומפט.

המסר הקבוע:

- זה מאגר בדיקות מקור.
- אפשר לחפש בדיקות שכבר פורסמו.
- אפשר לפתוח בדיקה ולקבל שורה תחתונה, מקור, מה כן נכון ואיפה הקפיצה.
- אם אין בדיקה, אפשר להציע טענה לבדיקה עתידית.

בכל מקום שיש חיפוש או CTA, להימנע מניסוח שמרמז: "הכנס טענה וקבל תשובה מיידית".

## קישורי טקסט פנימיים

קישורי טקסט צריכים לעזור להתמצאות, לא להפוך כל פסקה למפת אתר.

כללים:

- להוסיף קישור רק כשהמילה כבר מסמנת פעולה ברורה: חיפוש, כל הבדיקות, הצעת טענה, תיקונים, מדיניות עריכה, שיטת הבדיקה.
- לא לקשר כל הופעה של אותה מילה באותו עמוד.
- לא לשים יותר מ-1-3 קישורי טקסט בעמוד סטטי קצר, אלא אם יש צורך ברור.
- להשתמש ב-`text-link` לקישור בתוך מלל רגיל.
- אם כבר יש כפתור מיד מתחת לפסקה, לא חייבים גם קישור טקסט, אלא אם זה עוזר להבנת המשפט.

## קבצי SEO/GEO

מצב נוכחי:

- `/sitemap.xml` מוגש מקובץ סטטי: `public/sitemap.xml`.
- `/robots.txt` מוגש מקובץ סטטי: `public/robots.txt`.
- `/llms.txt` מוגש דינמית מתוך `app/llms.txt/route.ts`.

המקור המרכזי לעמודי בדיקה הוא `claimRecords`. לכן פרסום תקין מתחיל שם, אבל כל עוד ה-sitemap סטטי צריך לוודא ידנית שעמוד חדש נוסף גם ל-`public/sitemap.xml`.

לא להחזיר `app/sitemap.ts` או `app/robots.ts` בלי החלטה מפורשת, כי המעבר לקבצים סטטיים נעשה כדי לצמצם בעיות קריאה ב-Google Search Console.

## תגיות וחיפוש חי

תגיות הן התשתית לחיפוש החי.

בשלב הנוכחי:

- לכל claim חייבות להיות 4-5 תגיות.
- התגיות מוצגות בכרטיסי הבית ובעמוד `/topics`.
- `/llms.txt` כולל תגיות כדי לעזור למנועי תשובות להבין הקשר.
- החיפוש החי נמצא ב-`components/ClaimSearch.tsx` ומחפש מקומית בתוך `claimRecords`.
- החיפוש צריך לעבוד בלי Enter: הקלדה או לחיצה על תגית מסדרות תוצאות מיד.
- החיפוש צריך להסביר Input / Output / תחום, כדי שלא ייראה כמו כלי AI פתוח.
- אם יש הרבה תגיות, להציג קודם תגיות מרכזיות ולפתוח את השאר דרך כפתור "עוד תגיות".

לא מוסיפים עדיין מנוע חיפוש חיצוני, דאטאבייס או AI Search. רק אם האתר יגדל משמעותית והחיפוש המקומי כבר לא יספיק.

## ניווט מובייל

- במובייל יש גם פעולות מהירות בשורה העליונה: חיפוש והצעה.
- הניווט התחתון נשאר כי הוא נוח לאגודל, אבל אסור שיהיה הדרך היחידה להגיע לחיפוש.
- אחרי כל שינוי ניווט לבדוק מסך קטן: 360px רוחב, שהכפתורים לא נמעכים ושאין גלילה אופקית.

## ניקוי טיפוגרפי

- בעמודים גלויים לציבור יש להעדיף מקף קצר רגיל: -
- לא להשתמש במקף ארוך כאשר הוא עלול להסיח את הקורא.
- אין צורך להילחם בכל סימן טיפוגרפי בקוד פנימי או בקבצי TODO.

## מדידה

כרגע האתר משתמש ב-Vercel Web Analytics בלבד.

## בדיקות טכניות לפני פריסה

1. `npm ci`
2. `npm test`
3. `npm run check:content`
4. `npm run typecheck`
5. `npm run build`
6. `npm audit --omit=dev`

כותרות האבטחה מנוהלות ב-`next.config.ts`. שינוי ב-CSP מחייב בדיקה של Analytics, תמונות שיתוף וקישורים חיצוניים.

אם מוסיפים PostHog בעתיד:

- בלי autocapture.
- בלי session recordings.
- בלי פרופיילינג אישי.
- בלי שמירת טקסט חופשי.
- בלי שמירת URLים רגישים.
- למדוד רק פעולות ברורות: פתיחת מקור, העתקת תגובה, מעבר לעמוד בדיקה.

## וידאו

Remotion אינו חלק פעיל מ-main כרגע.

אם מחזירים Remotion:

- לעשות זאת בענף נפרד.
- לוודא שהשימוש ברור: יצירת סרטוני הסבר או קליפים קצרים מהתוכן.
- לא לערבב עם PostHog או שדרוג Next.

## שדרוג Next.js

שדרוג Next הוא שינוי תשתיתי.

כללים:

1. לעשות בענף נפרד.
2. לא לערבב עם תוכן או מדידה.
3. להריץ typecheck.
4. להריץ build.
5. לבדוק עמוד בית, עמוד בדיקה, sitemap, robots ו-llms.txt.
6. רק אז למזג.
